const { getTitle } = require('./title');
const { getTime } = require('./time');
const { getContent,formatContent } = require('./content');

module.exports = {
  getTitle,
  getTime,
  getContent,
  formatContent
};
